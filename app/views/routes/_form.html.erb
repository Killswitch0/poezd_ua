<%= form_with(model: @route) do |form| %>
  <%= render 'common/errors', resource: @route %>

  <div>
    <%= form.label 'Название', style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :railway_station_ids, "Список станций" %>
    <%= form.collection_select :railway_station_ids, RailwayStation.all, :id, :title, {}, { multiple: true } %>
  </div>

  <div>
    <%= form.submit "#{ @route.new_record? ? 'Создать' : 'Изменить' }" %>
  </div>
<% end %>
