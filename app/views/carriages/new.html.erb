<h1>Новый вагон</h1>

<!-- так как ресурс вложенный, мы сначала пишем родительский - @train,
  а затем вложенный @carriage", и делаем это в массиве.
  Поля в форме будут относиться непосредственно к объекту @carriage -->
<%= form_for [@train, @carriage] do |f| %>
  <div>
    <%= f.label :number, 'Номер' %><br>
    <%= f.text_field :number %>
  </div>

  <div>
    <%= f.label :top_seats, 'Верхние сидения' %><br>
    <%= f.text_field :top_seats %>
  </div>

  <div>
    <%= f.label :bottom_seats, 'Нижние сидения' %><br>
    <%= f.text_field :bottom_seats %>
  </div>

  <div>
    <%= f.label :side_top_seats, 'Верхние боковые сидения' %><br>
    <%= f.text_field :side_top_seats %>
  </div>

  <div>
    <%= f.label :side_bottom_seats, 'Верхние нижние сидения' %><br>
    <%= f.text_field :side_bottom_seats %>
  </div>

  <div>
    <%= f.submit 'Создать' %>
  </div>
<% end %>

